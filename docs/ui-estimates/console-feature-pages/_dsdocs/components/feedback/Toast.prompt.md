Toast is a dark transient confirmation pill; position it fixed bottom-center yourself and dismiss after ~4s.

```jsx
<Toast tone="success">Changes saved</Toast>
```

Copy is terse, no punctuation ("Changes saved", "Key rotated").
