Tabs is an underline tab bar for switching views within a page — active tab gets a 2px clay underline.

```jsx
<Tabs items={[{id:"keys",label:"API keys",count:3},"Members"]} value={tab} onChange={setTab} />
```
