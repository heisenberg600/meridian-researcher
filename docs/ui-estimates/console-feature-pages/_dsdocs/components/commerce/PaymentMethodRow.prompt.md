One saved payment method in a billing list — text brand plate (no drawn card logos), masked number, expiry, Default badge, hover-revealed Make default/Remove actions. Stack rows with hairline dividers inside a Card.

```jsx
<PaymentMethodRow brand="visa" last4="4242" expiry="08/27" isDefault />
<PaymentMethodRow brand="upi" last4="9931" onMakeDefault={mk} onRemove={rm} />
```
