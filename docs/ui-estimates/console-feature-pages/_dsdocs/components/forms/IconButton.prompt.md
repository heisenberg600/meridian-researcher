IconButton is a square icon-only control for toolbars, table rows, and card corners — always give it an aria-label.

```jsx
<IconButton aria-label="Copy key"><CopyIcon size={16} /></IconButton>
```

Variants: ghost (default, transparent) and secondary (bordered). Same sizes as Button.
