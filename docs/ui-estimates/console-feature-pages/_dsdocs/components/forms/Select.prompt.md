Select is a styled native dropdown with a chevron affordance.

```jsx
<Select label="Region" options={["us-east", "eu-west", "ap-south"]} />
```

Options may be strings or { value, label }. Same field styling as Input.
