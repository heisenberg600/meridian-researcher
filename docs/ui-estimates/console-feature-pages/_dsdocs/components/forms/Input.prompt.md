Input is the standard single-line text field with optional label, hint, and error.

```jsx
<Input label="Workspace name" placeholder="acme-prod" hint="Lowercase, hyphens allowed" />
<Input label="Email" error="Enter a valid email" />
```

36px tall, 6px radius. Error replaces the hint. Labels sentence case.
