Checkbox for multi-select lists and boolean form rows; 18px box, clay fill when checked.

```jsx
<Checkbox label="Email me weekly usage summaries" defaultChecked />
```

Works controlled (checked + onChange) or uncontrolled (defaultChecked).
