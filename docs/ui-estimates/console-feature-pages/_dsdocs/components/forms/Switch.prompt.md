Switch for settings that apply immediately (no save button); 34×20px pill.

```jsx
<Switch label="Require SSO for all members" defaultChecked onChange={setSso} />
```

Use Checkbox instead when the change is part of a form that gets submitted.
