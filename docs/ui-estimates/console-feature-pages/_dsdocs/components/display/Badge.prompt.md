Badge is a pill status label — use for states like Active, Pending, Failed.

```jsx
<Badge tone="success" dot>Active</Badge>
<Badge tone="warning">Pending</Badge>
```

Tones map to the semantic palette; "neutral" for non-status labels.
