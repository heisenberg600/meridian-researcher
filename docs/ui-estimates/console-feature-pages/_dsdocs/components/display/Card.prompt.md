Card is the default content surface — white, 12px radius, hairline border, xs shadow.

```jsx
<Card title="Usage" action={<Button variant="ghost" size="sm">Export</Button>}>…</Card>
```

Set padded={false} when the body is a table or list that should run edge-to-edge.
